@page
@model WebAppRazorPages.Pages.MotorcyclesModel
@{
    ViewData["Title"] = "Motorcycles page";
}

<html>
    <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" >
    <style>
        body{
            padding: 20px;
        }

        .car-card {
            border: 1px solid #dee2e6;
            border-radius: 4px;
            padding: 20px;
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Страница информации о ТС</h1>
        <a asp-page="EditMotorcycle" class="btn btn-primary btn-create mb-3">Создать</a>
        <div class="row">
            @foreach (var Motorcycle in Model.motorcycle)
            {
                <div class="col-md-4">
                    <div class="motorcycle-card">
                        <p><strong>ID:</strong>@Motorcycle.Id</p>
                        <p><strong>BrandMotorcycle:</strong>@Motorcycle.BrandMotorcycle</p>
                        <p><strong>Model:</strong>@Motorcycle.Model</p>
                        <p><strong>EngineMotorcycle:</strong>@Motorcycle.EngineMotorcycle</p>
                        <div>
                            <a asp-page="EditMotorcycle" asp-route-id="@Motorcycle.Id" class="btn btn-secondary">Изменить</a>
                            <form asp-page-handler="Delete" method="post" asp-route-id="@Motorcycle.Id" class="delete-form">
                                <button type="submit" class="btn btn-danger delete-button">Удалить</button>
                            </form>
                        </div>   
                    </div>
                </div>
            }
        </div>
    </div>
</body>
</html>